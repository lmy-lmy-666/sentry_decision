# sentry_decision 剩余待完成事项

> 更新: 2026-07-08 (三轮审查后)



---

## 一、电控（STM32 固件）

### 1. 0xB6 → 0x0120 下行

ROS 端已通过：`sentry_decision` → `sentry/command` → `serial_driver` → `0xB6`

STM32 侧需要新增：

```
收到 0xB6 包 → 解包 → 拼成 0x0120 sentry_cmd → 发给裁判系统
```

当前仅 `stance_command` 被决策填充，其余字段预留。

### 2. 0x020D → ROS 上行

裁判系统下发 0x020D（哨兵自主决策信息同步）。STM32 需要新增自定义上行包转发给 ROS 端 `serial_driver`。

### 3. 双方基地血量/总伤害量 → ROS 上行

当前 STM32 只转发己方数据，缺少对方基地血量和总伤害量。用于后期判断领先/落后。

### 4. 0x020C → ROS 上行（可选）

空中威胁、双倍易伤标记。有则更好，暂无也不影响基础功能。

---

## 二、自瞄

### 结构化 Target topic

当前决策通过 String 解析自瞄数据。结构化 `rm_interfaces/msg/vision/Target` topic 已预留（自瞄端 `io/ros2/publish2nav.hpp` 有 `#ifdef` 开关），但哨兵实际入口未打开。`sentry.yaml` 已配置 `enable_target_msg: true`，需自瞄同学改 C++ 入口 `io::ROS2 ros2(true)`。

---

## 三、Profile 坐标

需实车地图确认后填入：

| 坐标 | 用途 |
|------|------|
| `supply` | 补给区目标点 |
| `retreat` | 撤退安全点（当前 RETREAT 直接去补给区） |
| `safe_cover` | 防空掩体 |
| `patrol` | 巡逻路点 |
| `attack_push` | 前压路点 |
| `defend_fallback` | 防守搜索路点 |
| `backup_supply_points` | 备用补给点（可选） |
| `backup_retreat_points` | 备用撤退点（可选） |

---

## 四、代码端（已完成 ✅）

三轮全面代码审查共修复 **15 个独立问题 (17 处代码改动)**，42/42 单元测试通过。详见 `README.md` 第八节和 `docs/changes/` 下的记录。

- [x] 代码逻辑性: P1 stale goal 竞态, N2 子状态 goal 统一重置, N3 雷达距离同步, 冗余清理
- [x] 运行稳定性: N1 cancel_nav 全取消, N4 feedback goal_id 比较, P5 解析 DoS, P6 robot_type 日志, P4 线程安全文档
- [x] 决策逻辑性: P2 late_game_trailing 接入, P7 under_attack 0.5s 持久化窗口, P8 TRACK 无 odom 空转
- [x] 决策稳定性: P9 RFID 3-tick 防抖, P10 RESUPPLY nav_stuck 快速响应
- [x] 整体鲁棒性: P3 safe_cover 校验, P13 PATROL 空路由保护

## 五、已验证

- [x] 裁判数据流（外部学校 bag + 决策正常切换状态）
- [x] 雷达数据流（雷达测试 bag + EnemyInfo 正常填充）
- [x] 单元测试 42/42 通过
- [x] 三轮全面代码审查（15 个问题全部修复，0 已知 bug）

## 六、待闭环验证

详见 `ANALYSIS.md` 第 13 节（8 个需实战/仿真验证的风险点）。

## 七、已知远期/依赖项

| 项目 | 状态 | 阻塞方 |
|------|------|--------|
| `stance_expiring()` | stub，始终返回 false | 0x020D 下行未就绪 |
| 多敌人决策 | 当前仅追踪最近敌人 | 远期规划 |
| 己方队友协同 | 远期规划 | — |
| 热管理优化 | 远期规划 | — |
| RFID 增益点策略 | 远期规划 | — |
